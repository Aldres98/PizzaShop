<h2>Your cart page:</h2>

<div id="cart">
<table class="table table-striped">
    <thead>
        <tr>
            <th>Name</th>
            <th>Price</th>
            <th>Quantity</th>
            <th>Total Item price<tn>
        </tr>
    </thead>
    <tbody>
    <% total_price = 0 %>
    <% total_quantity = 0 %>
    <% @items.each do |item| %>
        <tr>
            <td><%= item[0].title %></td>
            <td><%= item[0].price %></td>
            <td><%= item[1] %></td>
            <td> <%= item[1].to_i * item[0].price.to_i %>
        </tr>
        <% total_price += item[0].price * item[1].to_i %>
        <% total_quantity += item[1].to_i %>
    <% end %>

    <tr>
        

        <td></td>
        <td>Total price: <%= total_price %></td>
        <td>Total Quantity: <%= total_quantity %></td>


    </tr>
     </tbody>
</table>

<form action="/place_order" method="POST" role="form">

 <input type="hidden" name="order[orders_input]"  value="<%= @orders_input %>">

 <div class="form-group">
    <label>Name:</label>
    <input name="order[name]"  type="text" class="form-control" placeholder="Ваше имя" >
  </div>

  <div class="form-group">
    <label>Phone:</label>
    <input name="order[phone]" type="text" class="form-control" placeholder="Ваш телефон" >
  </div>

  <div class="form-group">
    <label>Address:</label>
    <input name="order[address]" type="text" class="form-control" placeholder="Ваш адрес: ">
  </div>

  <button type="submit" class="btn btn-success btn-lg">Place order</button>
  <button class="btn btn-danger btn-lg" onclick="return cancel_order()">Cancel</button>


</form>
    </div>
<!--   <button class="btn btn-success btn-lg" onclick="clear_cart();">Clear cart</button>
 -->    
